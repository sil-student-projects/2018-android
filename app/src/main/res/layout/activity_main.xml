<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="org.sil.gatherwords.MainActivity">

    <include
        android:id="@+id/app_action_support_bar"
        layout="@layout/app_action_support_bar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_editor_absoluteY="0dp" />

    <ListView
        android:id="@+id/session_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/create"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:clickable="true"
        android:onClick="onCreateSessionClick"
        android:src="@drawable/ic_add_white_24dp"
        app:backgroundTint="@android:color/holo_green_dark"
        app:borderWidth="0dp"
        app:layout_anchor="@id/session_list"
        app:layout_anchorGravity="bottom|right|end" android:focusable="true" />
</android.support.design.widget.CoordinatorLayout>
